# Claude Code local state
.claude/

# macOS
.DS_Store

# Environment secrets
.env
.env.local
*.local.json

# Reference source artefacts — large EUR-Lex HTML downloads and official PDF
# editions (e.g. BR Toolbox chapters) summarised into committed references/*.md
# files; keep the sources out of the repo.
plugins/**/references/*.html
plugins/**/references/*.pdf
plugins/**/references/br-ch*.txt

# Python build/conversion tooling cache
__pycache__/
*.pyc

# Generated by scripts/extract-skills.py (rebuilt in CI for GitHub Pages);
# run the script before scripts/serve.py for local preview.
site/prompts.json
