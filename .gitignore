# Claude Code local state
.claude/

# macOS
.DS_Store

# Environment secrets
.env
.env.local
*.local.json

# Reference source artefacts — large EUR-Lex HTML downloads converted into the
# committed staff-regulations-*.md files; keep the source out of the repo.
plugins/**/references/*.html

# Python build/conversion tooling cache
__pycache__/
*.pyc

# Generated by scripts/extract-skills.py (rebuilt in CI for GitHub Pages);
# run the script before scripts/serve.py for local preview.
site/prompts.json
